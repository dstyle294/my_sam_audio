## Motivation
To explore different model architectures, hyperparameter combinations and pooling methods automatically.

## Description
Allows you to input sweep parameters, including pooling types, number of epochs, learning rates, etc and save results in a csv file which you provide via a CLI argument. 