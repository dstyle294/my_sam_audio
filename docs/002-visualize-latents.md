## Motivation
The latents generated by SAM are a blackbox. We need some tool to visualize these results. 

## Workflow
Pass in the latents file, and get plots about how many of each class is in the dataset, PCA, 